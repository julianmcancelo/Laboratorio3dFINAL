# ⚠️ ARCHIVO DEPRECADO

Este endpoint `/api/auth/registro` está deprecado.

## ✅ Usar en su lugar:
```
POST /api/auth/register
```

El archivo `route.ts` en este directorio tiene una implementación antigua que usa `ejecutarConsulta` y no es compatible con Prisma.

Use `/api/auth/register` que tiene la implementación correcta.
