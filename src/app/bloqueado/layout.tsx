/**
 * Layout específico para la página de bloqueado
 * Sin navbar ni componentes adicionales - solo el contenido
 */

export default function BloqueadoLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
